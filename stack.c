#include "monty.h"
/**
 * f_stack - prints the top
 * @head: stack head
 * @counter: line number
 * Return:n o return
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
